import { AumentoTamanoTextoDirective } from './aumento-tamano-texto.directive';

describe('AumentoTamanoTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new AumentoTamanoTextoDirective();
    expect(directive).toBeTruthy();
  });
});
